<table>
<tr><td>
    <% results.each do |job_state| %>
        <% if (job_state.status_class == 6) %>
                    <% if job_state.content_name =~ /^No model built/ %>
                    <h2><%= job_state.content_name %></h2>
                     <%  else %>
                    <%= render :partial => 'layouts/pdb_result', :locals => {:pdb_result => job_state, :bgcolor => "#ccc"} %>
                    <% end %>
         <% end %>
     <% end %>
</td></tr>
<tr><td>Model is generated by selecting the best scoring hits using PSI-BLAST, pGenTHREADER and HHPred against the current PDB</td></tr>
</table>
